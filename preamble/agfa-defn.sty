%% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Definitionen
%% Stand: 2023/01/05
%% agfa-defn.sty
%% Die üblichen einfachen Abkürzungen
%% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\NeedsTeXFormat{LaTeX2e}											
\ProvidesClass{agfa-defn}
%% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Initialisierungen
%% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%% -- var-Symbole für griechisches Alphabet
%% --
\AtBeginDocument{%
\let\ORGvarepsilon=\epsilon
\let\epsilon=\epsilon
\let\epsilon=\ORGvarepsilon
%
\let\ORGvarrho=\rho
\let\rho=\rho
\let\rho=\ORGvarrho
%
\let\ORGvartheta=\theta
\let\theta=\theta
\let\theta=\ORGvartheta
%
\let\ORGvarphi=\phi
\let\phi=\phi
\let\phi=\ORGvarphi
%
\let\ORGvarleq=\leqslant
\let\leqslant=\leq
\let\leq=\ORGvarleq
%
\let\ORGvargeq=\geqslant
\let\geqslant=\geq
\let\geq=\ORGvargeq
%
\let\ORGvarpreccurlyeq=\preccurlyeq
\let\preccurlyeq=\preceq
\let\preceq=\ORGvarpreccurlyeq
%
\let\setminus\smallsetminus			% A - B fuer Mengen
\renewcommand{\P}{\mathfrak{P}}		% Achtung: \P hat andere Bedeutung in LaTeX
}

%% -- Eins einer Algebra
\RequirePackage{bbm}
\newcommand{\1}{\mathbbm{1}}
\newcommand{\e}{\mathbbm{1}}

%% -- Zahlen
\newcommand{\N}{\mathbb{N}}		% Natuerliche Zahlen
\newcommand{\Z}{\mathbb{Z}}		% Ganze Zahlen
\newcommand{\Q}{\mathbb{Q}}		% Rationale Zahlen
\newcommand{\R}{\mathbb{R}}		% Reelle Zahlen
\newcommand{\C}{\mathbb{C}}		% Komplexe Zahlen
\newcommand{\K}{\mathbb{K}}		% Koerperzeichen
\newcommand{\T}{\mathbb{T}}


%% -- Differential Allgemeine Abkuerzungen
\newcommand*{\diff}[1]{\mathop{}\!\mathrm{d}{#1}}	% \diff{s} = ds, 
\renewcommand*{\d}[1]{\mathop{}\!\mathrm{d}{#1}}	% \d{\mu} = d.., 
\newcommand*{\ds}{\mathop{}\!\mathrm{d}{s}}       	% \ds = ds, 
\newcommand*{\dg}{\mathop{}\!\mathrm{d}{g}}       	% \dg = dg, 
\newcommand*{\dt}{\mathop{}\!\mathrm{d}{t}}       	% \dt = dt, 
\newcommand*{\dx}{\mathop{}\!\mathrm{d}{x}}       	% \dx = dx, 
\newcommand*{\dy}{\mathop{}\!\mathrm{d}{y}}       	% \dy = dy, 
\newcommand*{\dr}{\mathop{}\!\mathrm{d}{r}}       	% \dx = dr, 

%% -- Inverse fuer Mengenfunktionen
\newcommand{\finv}[1]{\stackrel{-1}{#1}}  % 

%% -- Eulersche Zahl und imaginäre Einheit
\newcommand{\im}{\ensuremath{\mathrm{i}}}
\newcommand{\eu}{\ensuremath{\mathrm{e\;\!}}}

%% -- Bezeichnungen
\newcommand*{\sig}{\mathop{}\!\mathrm{sign}\,}  % \sign
\newcommand*{\lin}{\mathop{}\!\mathrm{lin}}		%  Linearer Aufspann
\newcommand*{\conv}{\mathop{}\!\mathrm{conv}}	% Konvex

%% -- Operatoren

\newcommand*{\pr}{\mathop{}\!\mathrm{pr}}   		% Projektion
\newcommand{\tr}[1]{\mathop{}\mathrm{Tr}(#1)}		% Spur
\newcommand{\Kern}[1]{\mathop{}\mathrm{ker}(#1)\,}	
\newcommand{\Bild}[1]{\mathop{}\mathrm{im}(#1)\,}	 % Bild
\newcommand{\Fix}[1]{\mathop{}\mathrm{Fix}(#1)}
%
\DeclareMathOperator{\dom}{dom} 					% Definitionsbereich
\DeclareMathOperator{\ran}{ran} 					% Bildbereich
\DeclareMathOperator{\codim}{codim}
\DeclareMathOperator{\supp}{supp}
\DeclareMathOperator{\esssup}{ess\,sup}



%% --

\endinput